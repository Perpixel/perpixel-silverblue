# Hardware video acceleration (AMD / Mesa)
# radeonsi VA-API driver is provided by mesa-va-drivers-freeworld (RPM Fusion),
# which ships the codecs the stock Fedora package strips. Verify with `vainfo`.
LIBVA_DRIVER_NAME=radeonsi
